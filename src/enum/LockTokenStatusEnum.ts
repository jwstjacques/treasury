/* istanbul ignore file */

export enum LockTokenStatusEnum {
  Invalid = "Invalid lock token.",
  IsValid = "Is valid.",
  LockReleased = "Lock has been released.",
  LockTokenDoesNotExist = "Lock token does not exist.",
  LockTokenExpired = "Lock token has expired.",
  Success = "Success",
}
